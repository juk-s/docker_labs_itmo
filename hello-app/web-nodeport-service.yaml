apiVersion: v1
kind: Service
metadata:
  name: web-nodeport-service
  labels:
    app: hello-app
    service-type: nodeport
spec:
  type: NodePort
  selector:
    app: hello-app
  ports:
  - name: http
    port: 80
    targetPort: 8000
    nodePort: 30080